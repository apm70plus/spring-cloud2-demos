spring:
  application:
    name: cloud-gateway
  config:
    name: cloud-gateway
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      username: config
      password: config123

